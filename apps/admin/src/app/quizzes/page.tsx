import { EmptyState } from "@/components/ui";

export default function QuizzesPage() {
  return <EmptyState title="Quiz builder" body="Quiz authoring is available through the backend admin API and can be expanded into a rich form workflow." />;
}
